# node_modules is NOT copied, and that is not just about image size: pnpm
# resolves @remotion/compositor-* per platform, so a macOS tree copied into a
# Linux image ships the darwin binary and every render fails to launch.
node_modules
.next
.remotion-bundle
.git
.env*
!.env.example
.lighthouse
social-exports
out
*.log
